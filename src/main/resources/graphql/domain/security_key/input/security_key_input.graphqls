input SecurityKeyInput{

    # Relations
    deviceId: ID!
    ownerId: ID!

    # Core fields
    name: String!
    securityType: SecurityType!
    serialNumber: String
    publicKey: String!
    keySource: KeySource!
    status: KeyStatus!

    # Optional lifecycle fields
    revokedTimestamp: String

}
