enum DeviceStatus{
    # Core Operational states
    ONLINE,
    OFFLINE,
    MAINTENANCE,
    ERROR,
    NEW,
    UNKNOWN,
    # Lifecycle states
    COMMISSIONING,
    STANDBY,
    DECOMMISSIONED,
    RETIRED
}