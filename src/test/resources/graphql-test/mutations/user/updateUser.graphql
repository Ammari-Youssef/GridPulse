mutation UpdateUser($id: UUID!, $input: UpdateUserInput) {
    updateUser(id: $id, input: $input) {
        __typename
        id
        firstname
        lastname
        email
        role
        updatedAt
    }
}