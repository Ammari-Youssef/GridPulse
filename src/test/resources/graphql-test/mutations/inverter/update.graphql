mutation UpdateInverter($id: UUID!, $input: InverterInput!) {
    updateInverter(id: $id, input: $input) {
        id
        name
        model
        version
        manufacturer
        createdAt
        updatedAt
        source
    }
}