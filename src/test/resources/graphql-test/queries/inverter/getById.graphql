query GetInverterById($id: UUID!) {
    getInverterById(id: $id) {
        id
        name
        model
        version
        manufacturer
        createdBy
        updatedBy
        createdAt
        updatedAt
        source
    }
}
