query GetCurrentUser {
    getCurrentUser {
        id
        email
        firstname
        lastname
        password
        role
        enabled
        createdBy
        updatedBy
        source
        createdAt
        updatedAt
    }
}
