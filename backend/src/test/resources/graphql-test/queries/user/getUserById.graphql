query GetUserById($id: UUID!) {
    getUserById(id: $id) {
        id
        email
        firstname
        lastname
        password
        role
        enabled
        createdBy
        updatedBy
        source
        createdAt
        updatedAt
    }
}
